use flake
dotenv_if_exists .env

# Podman/buildah use /var/tmp for layer staging which lives on / (full on this machine).
# Redirect to /home which has plenty of space.
export TMPDIR="$HOME/.tmp/buildah"
mkdir -p "$TMPDIR"

