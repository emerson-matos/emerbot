# Web

## Tailwind CSS

Prefer Tailwind theme utilities over arbitrary values. Use `min-w-24` instead of
`min-w-[96px]`, `text-xs` instead of `text-[11px]`, etc. Only use arbitrary
values when no equivalent Tailwind token exists.
